var swagger = require('./swagger.json');
var stt = require('../../index.js');
var config = {
  assertionFormat: 'should',
  testModule: 'supertest',
  pathName: ['/pets/findByStatus'],
  loadTest: [{pathName:'/pets/findByStatus', operation:'get', load:{requests: 10, concurrent: 1}}, { /* ... */ }],
  maxLen: 80
};

// Generates an array of JavaScript test files following specified configuration
console.log(stt.testGen(swagger, config));