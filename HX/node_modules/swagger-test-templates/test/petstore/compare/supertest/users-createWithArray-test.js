'use strict';
var chai = require('chai');
var supertest = require('supertest');
var api = supertest('http://petstore.swagger.wordnik.com'); // supertest init;

chai.should();

describe('/users/createWithArray', function() {
  describe('post', function() {
    it('should respond with default successful operation', function(done) {
      api.post('/v2/users/createWithArray')
      .set('Accept', 'application/json')
      .send({
        body: 'DATA GOES HERE'
      })
      .expect('DEFAULT RESPONSE CODE HERE')
      .end(function(err, res) {
        if (err) return done(err);

        res.body.should.equal(null); // non-json response or no schema
        done();
      });
    });

    it('should respond with default successful operation', function(done) {
      api.post('/v2/users/createWithArray')
      .set('Accept', 'application/json')
      .send({
        body: 'DATA GOES HERE'
      })
      .expect('DEFAULT RESPONSE CODE HERE')
      .end(function(err, res) {
        if (err) return done(err);

        res.body.should.equal(null); // non-json response or no schema
        done();
      });
    });

  });

});
